/** ######################### case convert #########################
 * * we can use toUpperCase() for upperCase
 * * we can use toLowerCase() for lowerCase
 * *
 */

let str = "Javascript";

let upperCase = str.toUpperCase();
console.log(upperCase);

let lowerCase = str.toLowerCase();
console.log(lowerCase);
