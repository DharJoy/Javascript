/** ######################### use strict #########################
 * * "use strict"; Defines that JavaScript code should be executed in "strict mode".
 */

"use strict";

//* Using an object,variable without declaring it, is not allowed

// x = 500; // ReferenceError: x is not defined

// func();
// function func() {
//   y = 200; // ReferenceError: y is not defined
// }

// person = { name: "Karim" };

//* Deleting a variable (or object) is not allowed.

// let x = 200;
// let a = { x: 100 };
// delete a;
